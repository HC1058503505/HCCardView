# Uncomment the next line to define a global platform for your project
 platform :ios, '10.3'

target 'HCCardView' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HCCardView
  pod 'Kingfisher', '~> 3.5.2'
  pod 'SnapKit', '~> 3.2.0'
end
